# RCPA Frontend

This directory contains the admin dashboard for RCPA.

- `npm run dev` starts the local frontend dev server.
- `npm run build` writes production assets to `frontend/dist`.
- The Rust server serves built assets from `frontend/dist`.
